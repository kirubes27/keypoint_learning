# keypoint_learning

This repository contains a minimal TDW Phase‑A dataset generator for producing ordered, fine‑step pose sequences of single rigid objects with aligned per-frame metadata, intended as a clean starting point for keypoint/representation-learning experiments based on temporal predictability and simple operators.
