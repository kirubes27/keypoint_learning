# Gate 0 execution task - representative-replay pre-gate

Read first: /Users/kirubeso.r/Documents/PhD/docs/consultations/2026-07-26-decision-synthesis/DECISION_SYNTHESIS_v2_2026-07-26.md (GATE 0 section AND v2.1 amendments 1-3) and READOUT_SPEC_v1.md in the same folder. Execute Gate 0 exactly as amended. You may write ONLY inside this folder (gate0_replay).

Steps:
1. Locate the representative-pilot frozen checkpoints and their validation frames with supervised targets. Search /Users/kirubeso.r/Documents/PhD/cluster_downloads/ (stage_r2_representative_pilot*) and /Users/kirubeso.r/Documents/PhD/keypoint_learning_fitted_operator/. Record exact paths used.
2. Read the model code (keypoint_net/model.py) and write a replay script here that loads the frozen checkpoint, computes per-channel heatmaps on the validation frames, and evaluates: current global expectation readout; local/windowed soft-argmax r in {2,4,8} per READOUT_SPEC_v1; cell-argmax as the mode-correctness probe. NO training, NO weight updates.
3. Apply the frozen v2.1 definitions verbatim: sample unit = channel-frame pair over all validation frames; high-error = per-pair coordinate error above the 75th percentile of the global-readout distribution; correct-dominant-mode = heatmap argmax cell within 1 cell of the supervised target cell. Stratify high-error pairs into correct-dominant-mode vs wrong-or-diffuse-mode. Report per-stratum counts over the fixed denominator, plus median and p90 per readout variant.
4. Write GATE0_REPLAY_RESULTS.md in this folder: paths used, script hash, per-stratum counts, per-variant error tables, and the decision-rule outcome (at least half of high-error pairs correct-dominant-mode = windowing keeps rank; otherwise it loses rank and the wrong-or-diffuse diagnosis routes to the diagnostic head). Descriptive only, one seed, per amendment 3.
5. HARD RULES: if checkpoints, validation frames, or supervised targets cannot be found or loaded, STOP and write GATE0_BLOCKED.md stating exactly what is missing and where you looked. NEVER fabricate numbers. NEVER write outside this folder. If torch or dependencies are missing, record the exact import error in GATE0_BLOCKED.md.
