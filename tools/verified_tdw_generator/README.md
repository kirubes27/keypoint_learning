# Verified TDW generator sources

This directory is the tracked runnable authority for the six recovered TDW
generator sources bound by archival commit
`2529013aa8469338e8b27b7bd989056445b0a267`.

Five Python files are byte-identical to the active recovered versions. Only
`generate_next_datasets.py` differs, and only in the three Phase-E-approved
path controls for the canonical roll input, canonical data output base, and
future semantic-lock generator path. Scientific generation logic is unchanged.

The complete immutable recovery and equivalence packet is preserved at:

`/Users/kirubeso.r/Documents/PhD/artifacts/source-recovery/verified-generator/`

See `SOURCE_BINDING.json` for exact hashes and provenance.
